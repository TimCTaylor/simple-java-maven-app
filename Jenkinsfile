pipeline {
    agent any
    tools { 
        maven 'maven-399'
    }
    stages {
        stage('Build') { 
            steps {
                sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}
